const express = require('express');
const router = express.Router();
const generatePdfController = require('../../Controller/Facture/generatepdf');

//router.get('/', generatePdfController.get);
module.exports = router;
